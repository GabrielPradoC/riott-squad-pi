export enum EnumConstants {
    CONNECTION_NAME = 'riott-api',
    SALT_ROUNDS = 10
}

// valores computados não podem ser declarados no enum acima.
export enum DateConstants {
    EIGHTEEN_YEARS_IN_MILISECONDS = 1000 * 60 * 60 * 24 * 365 * 18
}
